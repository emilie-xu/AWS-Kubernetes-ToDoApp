apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask-app
  template:
    metadata:
      labels:
        app: flask-app
    spec:
      containers:
      - name: flask-app
        image: ccbd-todo-app-flask:latest
        ports:
        - containerPort: 5000
        resources:
          limits:
            cpu: "0.5"
            memory: "256Mi"
          requests:
            cpu: "500m"
            memory: "256Mi"
      #   env:
      #   - name: FLASK_APP
      #     value: "app.py"
      #   - name: FLASK_ENV
      #     value: "production"
      #   - name: DATABASE_URL
      #     value: "your-database-url"
      #   - name: SECRET_KEY
      #     valueFrom:
      #       secretKeyRef:
      #         name: flask-app-secrets
      #         key: secret-key
      # imagePullSecrets:
      # - name: your-registry-secret
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: flask-app
# spec:
#   selector:
#     app: flask-app
#   ports:
#   - name: http
#     port: 80
#     targetPort: 5000
#   type: LoadBalancer
